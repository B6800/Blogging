services:
  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: asdf1234
      MARIADB_DATABASE: streamvaultapp
    volumes:
      - ./db-data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost", "--password=asdf1234"]

  adminer:
    image: adminer
    restart: always
    ports:
      - "7070:8080"
    depends_on:
      db:
        condition: service_healthy

  backend:
    image: streamvault/backend
    restart: always
    build:
      context: ./streamvault-backend
    expose:
      - "8080"
    depends_on:
      db:
        condition: service_healthy

  nginx:
    build:
      context: ./nginx
    image: streamvault/nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend